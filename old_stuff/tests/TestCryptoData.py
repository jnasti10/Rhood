from cryptoData import *
import random

d = cryptoData(1000)

new_positons = d.init_stats_dict()

#start with 0
for key, value in new_positions.items():
    new_positions[name] = 0

d.pre_update(new_positions)

#get random actions
actions = get_random_actions(d.cash, new_positions)

#assign random 
for key, value in new_positions:
    new_positions[name] = random.random()

#normalize

print(random.random())   

print(d)

def get_random_actions(extra_cash, current_positions):
    new_positions = cryptoData().init_stats_dict()
    total_current_position = extra_cash
    total_nex_position = 
    for key, value in new_positions.items():
        new_positions[name] = random.random()
        total_current_position = current_positions[name]

    expected_total = random.uniform(0, total_current_value)

    for 
